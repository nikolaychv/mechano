package bg.mechano.mechano.domain.enums;

/**
 * Represents the role assigned to a user within the platform.
 *
 * User roles define the level of access and permissions
 * available to different types of users.
 */
public enum UserRole {
    CLIENT,
    SERVICE_OWNER,
    ADMIN
}
