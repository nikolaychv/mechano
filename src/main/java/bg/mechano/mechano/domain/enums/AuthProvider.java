package bg.mechano.mechano.domain.enums;

/**
 * Represents the authentication provider used for user login.
 *
 * This enum defines the supported authentication mechanisms
 * within the platform, including local authentication and
 * external OAuth-based providers.
 */
public enum AuthProvider {
    LOCAL,
    GOOGLE
}
